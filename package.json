{
  "name": "maven-pom-editor",
  "displayName": "Maven POM Editor",
  "description": "Advanced Maven POM editor with dependency hierarchy visualization",
  "version": "0.1.0",
  "publisher": "Bokix",
  "icon": "images/icon.png",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Programming Languages",
    "Other"
  ],
  "keywords": [
    "maven",
    "pom",
    "xml",
    "dependency",
    "java",
    "build",
    "maven-pom",
    "dependency-management",
    "dependency-tree"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/bokix/maven-pom-editor.git"
  },
  "bugs": {
    "url": "https://github.com/bokix/maven-pom-editor.git/issues"
  },
  "homepage": "https://github.com/bokix/maven-pom-editor",
  "galleryBanner": {
    "color": "#2B5797",
    "theme": "dark"
  },
  "activationEvents": [
    "onCommand:mavenPomEditor.openPomView",
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "mavenPomEditor.openPomView",
        "title": "Open Maven POM View",
        "icon": "$(extensions)"
      },
      {
        "command": "mavenPomEditor.clearCache",
        "title": "Maven POM Editor: Clear All Cache"
      },
      {
        "command": "mavenPomEditor.showCacheStats",
        "title": "Maven POM Editor: Show Cache Statistics"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "mavenPomEditor.openPomView",
          "when": "resourceFilename == pom.xml",
          "group": "navigation@1"
        }
      ],
      "editor/context": [
        {
          "command": "mavenPomEditor.openPomView",
          "when": "resourceFilename == pom.xml",
          "group": "2_workspace@5"
        }
      ]
    },
    "taskDefinitions": [
      {
        "type": "maven",
        "required": [
          "goal"
        ],
        "properties": {
          "goal": {
            "type": "string",
            "description": "The Maven goal to execute (e.g., clean, compile, test, package, install)"
          },
          "pomFile": {
            "type": "string",
            "description": "Path to the pom.xml file"
          }
        }
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/node": "20.x",
    "@types/vscode": "^1.80.0",
    "@typescript-eslint/eslint-plugin": "^6.4.1",
    "@typescript-eslint/parser": "^6.4.1",
    "@vscode/vsce": "^2.32.0",
    "eslint": "^8.47.0",
    "typescript": "^5.1.6"
  }
}
